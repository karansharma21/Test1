#include<iostream>
using namespace std;
maxHamming(arr[],int N)
{
	int Hamming;
	for(i=1;i<=N;i++)
		for(j=1;j<=N;j++)
			if(B[i]!=A[i])
			Hamming++;
			
}

int main()
{
	int T,N;
	cin>>T;
	while(T--)
	cin>>N;
	int A[N];
	for(i=1;i<=N;i++)
		cin>>A[i];
	int B[N];
	maxHamming(arr,N);
	
}
